
open source project learning
    will reading code with [Beyond compare] and [source insight].
    will test code with demo.

[rule]
    annotation will set rabinhu flag.
    ??? meaning unknown.



-----------------------------------------------libevent
[focus]
    io
    thread
[version]
   libevent-0.1.tar.gz 
        ./configure --prefix=./
        vim Makefile, change '-g -O2' to '-ggdb3 -O0'
        make
    libevent-0.7c.tar.gz
        ./configure --prefix=/home/OSPL/event/libevent CFLAGS='-ggdb3 -O0'
        make
    libevent-0.8.tar.gz
        ./configure --prefix=/home/OSPL/event/libevent CFLAGS='-ggdb3 -O0'
        make
    libevent-1.2.tar.gz
        ./configure --prefix=/home/OSPL/event/libevent CFLAGS='-ggdb3 -O0' --enable-static=no
            //--enable-shared --enable-static default yes.
        make -j 4
    libevent-2.1.5-beta.tar.gz
        ./configure --prefix=/home/OSPL/event/libevent CFLAGS='-ggdb3 -O0' --enable-static=no --enable-verbose-debug=yes
        make -j 4

        


----------------------------------------------thrift
[focus]
    rpc
    protocol design
[version]

------------------------------------------------boost
[focus]
c++ feature


[version]
    boost_1_58_0.tar.gz
        ./bootstrap.sh   (can --help)
        ./b2 variant=debug link=shared  (can --help)
        ./b2 install



